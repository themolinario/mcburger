import isEmpty from './isEmpty';
import mapErrors from './mapErrors';
import readHTMLFile from './readHTMLFile';
import regexUsername from './regexUsername';
import sendMail from './sendMail';

const Utils = {
  isEmpty,
  mapErrors,
  readHTMLFile,
  regexUsername,
  sendMail,
};

export default Utils;
